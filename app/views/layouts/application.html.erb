<!DOCTYPE html>
<html>
  <head>
    <title>Conferência</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-100 font-sans">
    <nav class="bg-blue-600 text-white p-4">
      <div class="container mx-auto flex justify-between">
        <a href="/" class="text-2xl font-bold">Conferência</a>
        <div>
          <%= link_to "Palestras", talks_path, class: "mr-4 hover:underline" %>
          <%= link_to "Palestrantes", speakers_path, class: "mr-4 hover:underline" %>
          <%= link_to "Participantes", participants_path, class: "hover:underline" %>
        </div>
      </div>
    </nav>
    <div class="container mx-auto p-4">
      <% if notice %>
        <p class="text-green-600 bg-green-100 p-2 rounded mb-4"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="text-red-600 bg-red-100 p-2 rounded mb-4"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
